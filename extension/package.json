{
  "name": "claudeco-hawk-extension",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "HAWK-AI in the browser: the launch sniper, signed by the user's own Phantom, one approval per trade.",
  "engines": {
    "node": ">=22.13 <25"
  },
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node ../scripts/test-all.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  },
  "dependencies": {
    "@solana/web3.js": "1.98.4",
    "bs58": "4.0.1"
  }
}
